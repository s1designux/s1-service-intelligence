# 클라우드영상시스템 — 확인 필요 항목

> **작성 역할:** Service Guide Writer  
> **작성일:** 2026-05-21  
> **출처:** Figma 기획문서(10장) 기준 — 언급됐으나 상세 정의가 없거나, UX 설계 시 결정이 필요한 항목만 취합  
> **용도:** 담당 기획자·PO가 답변 후 서비스 가이드에 반영

---

## 확인 방법

각 항목에 아래 중 하나를 기입해 주세요:

- ✅ **confirmed** — 사실 확인됨 (답변 기입)
- ❌ **out-of-scope** — 이번 서비스 범위 외
- ⏳ **deferred** — 추후 결정 예정 (시점 기입)

---

## A. 서비스 구조 / 접근 주체

### A-01. 웹뷰어(클라우드 S/W) 접근 주체 `우선`

> 웹뷰어는 **에스원 내부 직원 전용**인가, **계약 고객(사업자)도 직접 로그인**하는가?

- 기획문서 파악: 고객 채널은 모두App(모바일)으로 명시. 클라우드 S/W(웹뷰어)가 에스원 내부 관제 도구인지, 고객도 접속 가능한지 불명확.
- 영향 범위: 화면 구성, 권한 체계, 로그인 방식 전체에 영향.

```
답변: 
```

---

### A-02. 모두App과 클라우드 S/W의 기능 경계 `우선`

> 어떤 기능은 모두App(고객 모바일)에서만, 어떤 기능은 클라우드 S/W(웹뷰어)에서만 제공하는가?

- 기획문서 파악: 두 채널 모두 언급되나 기능 경계 미정의.

```
답변: 
```

---

## B. 웹뷰어 화면 구성

### B-01. 클라우드 S/W 화면 목록 및 구조 `우선`

> 기획문서에 나열된 6개 기능(영상저장·장비이상감지·영상모니터링·녹화영상전송·영상검색·솔루션연동)이 웹뷰어에서 어떤 화면 구조로 구성되는가?

- 기획문서 파악: 기능 목록만 있고 화면 구조·내비게이션 미정의.

```
답변: 
```

---

### B-02. 영상 검색 화면 구성 `우선`

> "파란선초 및 빨간색모자를 쓴 사람" 같은 문장 검색 후 결과를 어떻게 표시하는가? 영상 구간 재생 UX는?

- 기획문서 파악: 문장 검색 기능과 예시 문장은 명시됨. 결과 화면 미정의.

```
답변: 
```

---

### B-03. AI 알고리즘 구독 관리 화면 `중간`

> 유료 AI 알고리즘 옵션(화재·배회·라인크로스·피플카운트)을 추가·관리하는 화면은 어디에 위치하는가?

```
답변: 
```

---

## C. 시스템 연동

### C-01. 관제서버 연동 — 요청출동 처리 흐름 `우선`

> 기본 제공 월 2회 요청출동이 웹뷰어에서 어떻게 처리되는가? (요청 → 접수 → 완료 흐름)

- 기획문서 파악: 관제서버 연동으로 "요청 출동 및 영상확인 관제" 제공이라고만 명시.

```
답변: 
```

---

### C-02. 통합계정 연동 방식 `중간`

> 에스원 통합계정과의 연동 방식은? (SSO, OAuth, 자체 인증 등)

```
답변: 
```

---

### C-03. 영상확인 관제서비스 (유료 옵션) 화면 `중간`

> 옵션인 "영상확인 관제서비스(10円)"가 웹뷰어에서 어떻게 동작하는가? 에스원 직원이 고객 영상을 원격으로 확인하는 방식인가?

```
답변: 
```

---

## D. 내부 운영 체계

### D-01. 에스원 내부 운영자 역할 체계 `중간`

> 클라우드 S/W를 사용하는 에스원 내부 직원의 역할은 어떻게 구분되는가?

- 기획문서 파악: "AI 관제", "영상확인 관제"가 언급되나 구체적 역할 구분 미정의.

```
답변: 
```

---

### D-02. 고객 온보딩 흐름 `낮음`

> 다이렉트몰 가입 후 카메라 설치 → 서비스 활성화까지의 고객 경험 흐름은 어떻게 되는가?

```
답변: 
```

---

## 취합 현황

| 코드 | 주제 | 우선순위 |
|------|------|----------|
| A-01 | 웹뷰어 접근 주체 | 우선 — 화면 설계 전 반드시 확인 |
| A-02 | App vs 웹 기능 경계 | 우선 — 화면 범위에 직접 영향 |
| B-01 | 웹뷰어 화면 목록·구조 | 우선 — IA 설계 시작 전 필요 |
| B-02 | 영상 검색 화면 구성 | 우선 — 핵심 차별 기능 |
| C-01 | 요청출동 처리 흐름 | 우선 — 핵심 서비스 흐름 |
| C-02 | 통합계정 연동 방식 | 중간 |
| B-03 | AI 알고리즘 구독 화면 | 중간 |
| C-03 | 영상확인 관제서비스 화면 | 중간 |
| D-01 | 내부 운영자 역할 체계 | 중간 |
| D-02 | 고객 온보딩 흐름 | 낮음 |

---

*이 문서는 Figma 기획문서(10장) 기반으로 작성됐습니다. 프로토타입 코드베이스 내용은 포함하지 않습니다.*
